

var gv = gv || {};

gv.LOBBY = gv.LOBBY || {};

gv.LOBBY.DEFAULT_PAGE = 2;

gv.LOBBY.NAVIGATION_BAR = {
    MOVE_UP_DURATION: 0.1,
}

gv.LOBBY.CARDPAGE = {
    NOT_ENOUGH_GOLD_NOTIFY: "Bạn không đủ vàng để nâng cấp",
    NOT_ENOUGH_FRAGMENT_NOTIFY: "Bạn không đủ thẻ để nâng cấp",
    NOTIFY_TIMEOUT: 3,
    SWAP_CARD_DURATION: 0.25,
}

gv.LOBBY.OPEN_CHEST_DELAY = 0.1;

gv.LOBBY.SHOP_DAILY_ITEM = []

gv.LOBBY.CLICK_TOUCH_MAX_DURATION = 125;